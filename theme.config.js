/** @type {const} */
const themeColors = {
  primary: { light: '#7FFF00', dark: '#7FFF00' }, // Lime green - UP token primary
  background: { light: '#ffffff', dark: '#0a0a0a' }, // White/Dark background
  surface: { light: '#f0f8f0', dark: '#1a1a1a' }, // Light green tint/Dark surface
  foreground: { light: '#1a1a1a', dark: '#ffffff' }, // Dark/Light text
  muted: { light: '#666666', dark: '#999999' }, // Muted text
  border: { light: '#e0e0e0', dark: '#333333' }, // Border color
  success: { light: '#22C55E', dark: '#4ADE80' }, // Green success
  warning: { light: '#FFD700', dark: '#FFD700' }, // Golden yellow - UP token accent
  error: { light: '#EF4444', dark: '#F87171' }, // Red error
};

module.exports = { themeColors };
